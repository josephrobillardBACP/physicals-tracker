{
  "functions": [
    {
      "source": "functions",
      "codebase": "physicals",
      "ignore": ["node_modules", ".git", "lib", "*.local"],
      "predeploy": ["npm --prefix functions run build"]
    }
  ],
  "firestore": {
    "rules": "firestore.rules"
  }
}
