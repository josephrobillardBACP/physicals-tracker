{
  "functions": [
    {
      "source": "functions",
      "codebase": "physicals",
      "ignore": ["node_modules", ".git", "lib", "*.local"],
      "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"]
    }
  ],
  "firestore": {
    "rules": "firestore.rules"
  }
}
